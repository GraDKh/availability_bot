{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch Debug",
            "program": "./target/debug/big_brother",
            "cwd": "${workspaceRoot}"
        }
    ]
}